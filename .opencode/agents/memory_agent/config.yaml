name: Memory and Architecture Engineer
description: Fix memory subsystem and architectural issues including scratchpad organization, memory access patterns, and transformer fundamentals
tools:
  - bash
  - read
  - write
  - edit
  - glob
  - grep
responsibilities:
  - Document and implement banked scratchpad SRAM with conflict avoidance
  - Fix memory access patterns for tensor operations (Q/K/V projections, attention, FFN)
  - Implement proper AXI4-Lite/AXI4 interfaces with timing optimization
  - Address missing transformer fundamentals: layer norms, residual connections, position embeddings
  - Standardize fixed-point format across all modules with specification document
  - Ensure proper dataflow in accelerated_transformer_block.v
priority: high